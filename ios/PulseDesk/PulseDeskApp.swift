import SwiftUI

@main
struct PulseDeskApp: App {
    var body: some Scene {
        WindowGroup {
            RootView()
                .tint(Color(red: 0.18, green: 0.45, blue: 0.72))
        }
    }
}
