{
  "name": "pulse-desk-android-rail",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android wrapper for Pulse Desk (LEFT vertical nav rail variant) — a thin WebView shell that loads the live web app with the rail UA token.",
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/android": "^6.2.0"
  }
}
